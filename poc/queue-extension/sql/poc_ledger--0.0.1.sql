/* <begin connected objects> */
/*
This file is auto generated by pgrx.

The ordering of items is not stable, it is driven by a dependency graph.
*/
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:82

    CREATE TABLE poc_test_rows (
        row_id          BIGSERIAL PRIMARY KEY,
        shard_idx       INTEGER  NOT NULL,
        slot_idx        INTEGER  NOT NULL,
        request_seq     BIGINT   NOT NULL,
        committer_tx_id BIGINT   NOT NULL,
        user_tx_xid     xid8     NOT NULL,
        pool_hash       BIGINT   NOT NULL,
        method_tag      SMALLINT NOT NULL,
        event_sku_id    BIGINT   NOT NULL,
        event_location_id BIGINT NOT NULL,
        event_qty       BIGINT   NOT NULL,
        qty_fake        BIGINT   NOT NULL,
        inserted_at     TIMESTAMPTZ NOT NULL DEFAULT clock_timestamp()
    );
    CREATE INDEX poc_test_rows_committer_tx_id_idx
        ON poc_test_rows (shard_idx, committer_tx_id);
/* </end connected objects> */

/* <begin connected objects> */
-- src/cost_method.rs:309
-- requires:
--   poc_test_rows_schema


    CREATE SEQUENCE poc_cost_layers_born_seq;

    CREATE TABLE poc_cost_layers (
        layer_id        BIGSERIAL PRIMARY KEY,
        sku_id          BIGINT NOT NULL,
        location_id     BIGINT NOT NULL,
        qty             BIGINT NOT NULL CHECK (qty > 0),
        unit_cost       BIGINT NOT NULL,
        born_at         TIMESTAMPTZ NOT NULL DEFAULT clock_timestamp(),
        born_seq        BIGINT NOT NULL DEFAULT nextval('poc_cost_layers_born_seq'),
        source_kind     TEXT NOT NULL,
        source_ref      BIGINT,
        committer_tx_id BIGINT NOT NULL DEFAULT 0,
        user_tx_xid     xid8 NOT NULL DEFAULT '0'::xid8
    );
    CREATE INDEX poc_cost_layers_pool
        ON poc_cost_layers (sku_id, location_id, born_at, born_seq);
    CREATE INDEX poc_cost_layers_user_tx
        ON poc_cost_layers (user_tx_xid);

    CREATE TABLE poc_cost_depletions (
        depletion_id    BIGSERIAL PRIMARY KEY,
        layer_id        BIGINT NOT NULL REFERENCES poc_cost_layers(layer_id),
        qty             BIGINT NOT NULL CHECK (qty > 0),
        unit_cost       BIGINT NOT NULL,
        consumed_at     TIMESTAMPTZ NOT NULL,
        consumed_seq    BIGINT NOT NULL,
        issue_id        BIGINT NOT NULL,
        method_used     TEXT NOT NULL,
        committer_tx_id BIGINT NOT NULL,
        user_tx_xid     xid8 NOT NULL,
        UNIQUE (issue_id, method_used, layer_id)
    );
    CREATE INDEX poc_cost_depletions_layer
        ON poc_cost_depletions (layer_id);
    CREATE INDEX poc_cost_depletions_issue
        ON poc_cost_depletions (issue_id);
    CREATE INDEX poc_cost_depletions_user_tx
        ON poc_cost_depletions (user_tx_xid);

    CREATE TABLE poc_cost_consumptions (
        consumption_id    BIGSERIAL PRIMARY KEY,
        sku_id            BIGINT NOT NULL,
        location_id       BIGINT NOT NULL,
        qty               BIGINT NOT NULL CHECK (qty > 0),
        applied_unit_cost BIGINT NOT NULL,
        consumed_at       TIMESTAMPTZ NOT NULL,
        consumed_seq      BIGINT NOT NULL,
        issue_id          BIGINT NOT NULL,
        method_used       TEXT NOT NULL,
        committer_tx_id   BIGINT NOT NULL,
        user_tx_xid       xid8 NOT NULL,
        UNIQUE (issue_id, method_used)
    );
    CREATE INDEX poc_cost_consumptions_pool
        ON poc_cost_consumptions (sku_id, location_id, consumed_at);
    CREATE INDEX poc_cost_consumptions_issue
        ON poc_cost_consumptions (issue_id);
    CREATE INDEX poc_cost_consumptions_user_tx
        ON poc_cost_consumptions (user_tx_xid);

    -- M2.3 AVG running aggregate per pool. Receipts UPSERT increment;
    -- the committer decrements after consumption INSERT (post-plan_apply
    -- hook in process_group). Snapshot helper reads running_value /
    -- running_qty under FOR UPDATE.
    CREATE TABLE poc_cost_avg (
        sku_id        BIGINT NOT NULL,
        location_id   BIGINT NOT NULL,
        running_qty   BIGINT NOT NULL DEFAULT 0 CHECK (running_qty >= 0),
        running_value BIGINT NOT NULL DEFAULT 0 CHECK (running_value >= 0),
        last_updated  TIMESTAMPTZ NOT NULL DEFAULT clock_timestamp(),
        PRIMARY KEY (sku_id, location_id)
    );

    -- M2.3 STD lookup. One row per SKU; PoC keeps it flat (no time-
    -- phased revisions). Production would version by effective_at.
    CREATE TABLE poc_standard_costs (
        sku_id       BIGINT PRIMARY KEY,
        unit_cost    BIGINT NOT NULL CHECK (unit_cost > 0),
        effective_at TIMESTAMPTZ NOT NULL DEFAULT clock_timestamp(),
        posted_by    TEXT
    );

    -- M3.2 (acct-4d4n.8) Q-A pool-lock candidates. Both tables share the
    -- same (sku_id, location_id) primary key; the committer acquires a
    -- row-lock here at the top of process_group when there is work to
    -- plan. The two flavours differ only in payload — `poc_pool_locks`
    -- carries an audit `lock_version` column; `poc_pool_lock_anchors`
    -- is minimal. The GUC `poc_ledger.pool_lock_mode` selects which
    -- table (or 'none' for the no-extra-SPI baseline that relies on
    -- the cost-table FOR UPDATE inside the snapshot builders).
    CREATE TABLE poc_pool_locks (
        sku_id       BIGINT NOT NULL,
        location_id  BIGINT NOT NULL,
        lock_version BIGINT NOT NULL DEFAULT 0,
        PRIMARY KEY (sku_id, location_id)
    );

    CREATE TABLE poc_pool_lock_anchors (
        sku_id       BIGINT NOT NULL,
        location_id  BIGINT NOT NULL,
        PRIMARY KEY (sku_id, location_id)
    );
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:975
-- poc_ledger::committer::poc_ledger_apply
CREATE  FUNCTION "poc_ledger_apply"(
	"sku_id" bigint, /* i64 */
	"location_id" bigint, /* i64 */
	"qty" bigint, /* i64 */
	"issue_id" bigint DEFAULT 0, /* i64 */
	"method" TEXT DEFAULT 'fifo' /* alloc::string::String */
) RETURNS TABLE (
	"shard_idx" INT,  /* i32 */
	"slot_idx" INT,  /* i32 */
	"request_seq" bigint,  /* i64 */
	"committer_tx_id" bigint,  /* i64 */
	"applied_unit_cost" bigint,  /* i64 */
	"applied_total_cost" bigint,  /* i64 */
	"error_code" INT  /* i32 */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_apply_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1295
-- poc_ledger::committer::poc_ledger_committer_tick
CREATE  FUNCTION "poc_ledger_committer_tick"(
	"shard_idx" INT, /* i32 */
	"batch_size_max" INT DEFAULT 1024 /* i32 */
) RETURNS bigint /* i64 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_committer_tick_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/cost_method.rs:425
-- poc_ledger::cost_method::poc_ledger_error_code_to_text
CREATE  FUNCTION "poc_ledger_error_code_to_text"(
	"code" INT /* i32 */
) RETURNS TEXT /* core::option::Option<&str> */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_error_code_to_text_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/lib.rs:349
-- poc_ledger::poc_ledger_hello
CREATE  FUNCTION "poc_ledger_hello"() RETURNS TEXT /* &str */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_hello_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1743
-- poc_ledger::committer::poc_ledger_inject_dead_committer
CREATE  FUNCTION "poc_ledger_inject_dead_committer"(
	"shard_idx" INT, /* i32 */
	"fake_pid" INT, /* i32 */
	"fake_acquired_at_ns" bigint /* i64 */
) RETURNS bool /* bool */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_inject_dead_committer_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1159
-- poc_ledger::queue::poc_ledger_max_slot_probe
CREATE  FUNCTION "poc_ledger_max_slot_probe"() RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_max_slot_probe_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1691
-- poc_ledger::committer::poc_ledger_orphan_recovery_tick
CREATE  FUNCTION "poc_ledger_orphan_recovery_tick"(
	"shard_idx" INT /* i32 */
) RETURNS TABLE (
	"outcome" TEXT,  /* alloc::string::String */
	"stale_pid" INT,  /* i32 */
	"recovered" INT,  /* i32 */
	"abandoned" INT  /* i32 */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_orphan_recovery_tick_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1235
-- poc_ledger::committer::poc_ledger_push_only
CREATE  FUNCTION "poc_ledger_push_only"(
	"sku_id" bigint, /* i64 */
	"location_id" bigint, /* i64 */
	"qty" bigint, /* i64 */
	"issue_id" bigint, /* i64 */
	"method" TEXT DEFAULT 'fifo' /* alloc::string::String */
) RETURNS TABLE (
	"shard_idx" INT,  /* i32 */
	"slot_idx" INT,  /* i32 */
	"request_seq" bigint  /* i64 */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_push_only_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1382
-- poc_ledger::committer::poc_ledger_receive
CREATE  FUNCTION "poc_ledger_receive"(
	"sku_id" bigint, /* i64 */
	"location_id" bigint, /* i64 */
	"qty" bigint, /* i64 */
	"unit_cost" bigint, /* i64 */
	"source_kind" TEXT DEFAULT 'receipt' /* alloc::string::String */
) RETURNS bigint /* i64 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_receive_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1423
-- poc_ledger::committer::poc_ledger_receive_avg
CREATE  FUNCTION "poc_ledger_receive_avg"(
	"sku_id" bigint, /* i64 */
	"location_id" bigint, /* i64 */
	"qty" bigint, /* i64 */
	"unit_cost" bigint /* i64 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_receive_avg_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1149
-- poc_ledger::queue::poc_ledger_requests_per_shard
CREATE  FUNCTION "poc_ledger_requests_per_shard"() RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_requests_per_shard_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1320
-- poc_ledger::queue::poc_ledger_ring_drain_one
CREATE  FUNCTION "poc_ledger_ring_drain_one"(
	"shard_idx" INT /* i32 */
) RETURNS INT /* core::option::Option<i32> */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_ring_drain_one_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1292
-- poc_ledger::queue::poc_ledger_ring_push
CREATE  FUNCTION "poc_ledger_ring_push"(
	"shard_idx" INT, /* i32 */
	"slot_idx" INT, /* i32 */
	"pool_hash" bigint, /* i64 */
	"kind_tag" INT /* i32 */
) RETURNS bigint /* core::option::Option<i64> */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_ring_push_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1467
-- poc_ledger::committer::poc_ledger_set_standard_cost
CREATE  FUNCTION "poc_ledger_set_standard_cost"(
	"sku_id" bigint, /* i64 */
	"unit_cost" bigint /* i64 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_set_standard_cost_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1361
-- poc_ledger::committer::poc_ledger_shard_committer_tx_seq
CREATE  FUNCTION "poc_ledger_shard_committer_tx_seq"(
	"shard_idx" INT /* i32 */
) RETURNS bigint /* i64 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_shard_committer_tx_seq_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1144
-- poc_ledger::queue::poc_ledger_shard_count
CREATE  FUNCTION "poc_ledger_shard_count"() RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_shard_count_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1344
-- poc_ledger::queue::poc_ledger_shard_depth
CREATE  FUNCTION "poc_ledger_shard_depth"(
	"shard_idx" INT /* i32 */
) RETURNS bigint /* i64 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_shard_depth_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1498
-- poc_ledger::committer::poc_ledger_shard_for
CREATE  FUNCTION "poc_ledger_shard_for"(
	"sku_id" bigint, /* i64 */
	"location_id" bigint /* i64 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_shard_for_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1328
-- poc_ledger::queue::poc_ledger_shard_head
CREATE  FUNCTION "poc_ledger_shard_head"(
	"shard_idx" INT /* i32 */
) RETURNS bigint /* i64 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_shard_head_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1353
-- poc_ledger::queue::poc_ledger_shard_next_request_seq
CREATE  FUNCTION "poc_ledger_shard_next_request_seq"(
	"shard_idx" INT /* i32 */
) RETURNS bigint /* i64 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_shard_next_request_seq_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1364
-- poc_ledger::queue::poc_ledger_shard_next_slot_seq
CREATE  FUNCTION "poc_ledger_shard_next_slot_seq"(
	"shard_idx" INT /* i32 */
) RETURNS bigint /* i64 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_shard_next_slot_seq_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1213
-- poc_ledger::queue::poc_ledger_shard_reset
CREATE  FUNCTION "poc_ledger_shard_reset"(
	"shard_idx" INT /* i32 */
) RETURNS bool /* bool */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_shard_reset_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1766
-- poc_ledger::committer::poc_ledger_shard_stats_all
CREATE  FUNCTION "poc_ledger_shard_stats_all"() RETURNS TABLE (
	"shard_idx" INT,  /* i32 */
	"head" bigint,  /* i64 */
	"tail" bigint,  /* i64 */
	"depth" bigint,  /* i64 */
	"committer_pid" INT,  /* i32 */
	"committer_tx_seq" bigint,  /* i64 */
	"next_request_seq" bigint,  /* i64 */
	"next_slot_seq" bigint  /* i64 */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_shard_stats_all_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1336
-- poc_ledger::queue::poc_ledger_shard_tail
CREATE  FUNCTION "poc_ledger_shard_tail"(
	"shard_idx" INT /* i32 */
) RETURNS bigint /* i64 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_shard_tail_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1223
-- poc_ledger::queue::poc_ledger_slot_acquire
CREATE  FUNCTION "poc_ledger_slot_acquire"(
	"shard_idx" INT /* i32 */
) RETURNS INT /* core::option::Option<i32> */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_slot_acquire_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:2091
-- poc_ledger::committer::poc_ledger_slot_leak_audit_tick
CREATE  FUNCTION "poc_ledger_slot_leak_audit_tick"(
	"shard_idx" INT /* i32 */
) RETURNS TABLE (
	"shard_idx" INT,  /* i32 */
	"scanned" INT,  /* i32 */
	"reclaimed" INT  /* i32 */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_slot_leak_audit_tick_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1262
-- poc_ledger::queue::poc_ledger_slot_mark_abandoned
CREATE  FUNCTION "poc_ledger_slot_mark_abandoned"(
	"shard_idx" INT, /* i32 */
	"slot_idx" INT /* i32 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_slot_mark_abandoned_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1247
-- poc_ledger::queue::poc_ledger_slot_mark_filled
CREATE  FUNCTION "poc_ledger_slot_mark_filled"(
	"shard_idx" INT, /* i32 */
	"slot_idx" INT /* i32 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_slot_mark_filled_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1277
-- poc_ledger::queue::poc_ledger_slot_recycle
CREATE  FUNCTION "poc_ledger_slot_recycle"(
	"shard_idx" INT, /* i32 */
	"slot_idx" INT /* i32 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_slot_recycle_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1348
-- poc_ledger::committer::poc_ledger_slot_recycle_after_read
CREATE  FUNCTION "poc_ledger_slot_recycle_after_read"(
	"shard_idx" INT, /* i32 */
	"slot_idx" INT /* i32 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_slot_recycle_after_read_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/committer.rs:1316
-- poc_ledger::committer::poc_ledger_slot_result
CREATE  FUNCTION "poc_ledger_slot_result"(
	"shard_idx" INT, /* i32 */
	"slot_idx" INT /* i32 */
) RETURNS TABLE (
	"state" INT,  /* i32 */
	"applied_unit_cost" bigint,  /* i64 */
	"applied_total_cost" bigint,  /* i64 */
	"committer_tx_id" bigint,  /* i64 */
	"error_code" INT  /* i32 */
)
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_slot_result_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1231
-- poc_ledger::queue::poc_ledger_slot_state
CREATE  FUNCTION "poc_ledger_slot_state"(
	"shard_idx" INT, /* i32 */
	"slot_idx" INT /* i32 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_slot_state_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1154
-- poc_ledger::queue::poc_ledger_slots_per_shard
CREATE  FUNCTION "poc_ledger_slots_per_shard"() RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_slots_per_shard_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1189
-- poc_ledger::queue::poc_ledger_test_advance_head_and_signal
CREATE  FUNCTION "poc_ledger_test_advance_head_and_signal"(
	"shard_idx" INT, /* i32 */
	"n" INT /* i32 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_test_advance_head_and_signal_wrapper';
/* </end connected objects> */

/* <begin connected objects> */
-- src/queue.rs:1170
-- poc_ledger::queue::poc_ledger_test_force_ring_full
CREATE  FUNCTION "poc_ledger_test_force_ring_full"(
	"shard_idx" INT /* i32 */
) RETURNS INT /* i32 */
STRICT
LANGUAGE c /* Rust */
AS 'MODULE_PATHNAME', 'poc_ledger_test_force_ring_full_wrapper';
/* </end connected objects> */

